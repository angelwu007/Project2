{{#each imgs}}
  <h4>{{this.name}}</h4>
<a href="/image/{{this.id}}"> <img src="{{this.path}}" alt=""></a>
  <p>{{this.description}}</p>
  <a href="/image/edit/{{this.id}}">Edit</a>
  {{!-- <form action="/image/edit/{{id}}" method='GET'>
      <button>Edit</button>
  </form> --}}
  <form action="/image/delete/{{id}}" method='POST'>
      <button>Delete</button>
  </form>

{{else}}
 <p>No photo has been added</p>
{{/each}}